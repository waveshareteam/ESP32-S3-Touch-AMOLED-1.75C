# Factory Firmware

This directory contains factory or recovery firmware binaries for the ESP32-S3-Touch-AMOLED-1.75C board.

These binaries are checked-in release artifacts. They are not source projects, are not rebuilt by CI, and should not be mixed with generated workflow outputs.

Use the source examples under `examples/` when changing buildable firmware behavior.